namespace UI.Settings
{
    // Alternative approach to using this kind of intermediate
    // enum would be to put the theme SO in a resources folder
    // so that it can be loaded through code.
    public enum Theme
    {
        Light,
        Dark
    }
}
